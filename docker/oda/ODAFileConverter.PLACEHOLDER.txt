Этот файл — заглушка, нужна ТОЛЬКО чтобы `COPY docker/oda/ODAFileConverter*`
в Dockerfile не падал с ошибкой "no such file or directory", когда реального
бинарника ещё нет. Сама заглушка ничего не делает и в контейнере игнорируется
(entrypoint.sh ищет `*.AppImage`, эта заглушка под маску не подходит).

Как добавить реальную конвертацию DWG→DXF внутри Docker-образа — см. README.md
в этой же папке.
