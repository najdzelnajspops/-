# Чек-лист выполнения

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` сделано и проверено.

## Фаза 0 — Фундамент

- [x] Создана структура проекта, скопированы (read-only источник) референсные фрагменты кода в `docs/reference_snippets/`.
- [x] Извлечена структурированная выжимка ТЗ → `docs/TASK_BRIEF.md`.
- [x] Архитектура: `docs/ARCHITECTURE.md` — компоненты и поток данных полного DWG/DXF-пайплайна по ТЗ (Service A). Документ изначально описывал ещё и второй, вспомогательный сервис (Service B) — по решению пользователя (2026-09-24) исключён из объёма проекта, см. `docs/OPEN_QUESTIONS.md`.
- [x] Алгоритм данных: `docs/DATA_ALGORITHM.md` — порядок сбора/сопоставления/выдачи + приём новых территорий + геопривязка.
- [x] План агентов: `docs/AGENTS_PLAN.md` — включая агента ОТК (технический контроль).
- [x] Дизайн отчёта: `docs/REPORT_DESIGN.md` — визуальная часть + протокол ОТК в отчёте.
- [x] Реестр норм (пока без верифицированных пунктов) → `docs/NORMATIVE_REFERENCES.md`, включая кандидатов СНиП/ГОСТ.
- [x] Открытые вопросы → `docs/OPEN_QUESTIONS.md`.
- [x] Правила работы проекта → `AGENTS.md` (в т.ч. запрет упоминания внешних личных проектов пользователя в документации).

## Фаза 1 — Изучение реальных данных

- [x] Получен и изучен реальный датасет («Датасет.zip» → «Пилотный проект 20 улиц», 20 объектов, ~3986 файлов) — структура и выводы в `docs/DATA_STRUCTURE.md`.
- [x] Получен официальный список нормативных актов от организаторов (`Нормативные акты ссылки.docx`) — внесён в `docs/NORMATIVE_REFERENCES.md` (статус `identified_by_organizers`).
- [x] Найден и сохранён (сырой вид) официальный справочник видов растений с кодом ценности/компенсационной стоимостью → `data/reference/raw_species_directory_strings.txt`.
- [x] **Критично, проверено вживую**: DWG-файлы реально конвертированы в DXF через ODA File Converter (Windows-инсталлятор скачан, установлен, headless-конвертация двух реальных DWG прошла успешно, `ezdxf` открыл оба результата) — см. `docs/DATA_STRUCTURE.md` §9. Для Docker-образа ещё нужно проверить именно Linux `.deb`-сборку (сейчас проверено на Windows).
- [x] Определена система координат: единицы — метры (`INSUNITS=6`), но координаты локальные/условные (не сразу МСК-77) — см. `docs/DATA_STRUCTURE.md` §9, `docs/OPEN_QUESTIONS.md`.
- [x] Реальные имена слоёв коммуникаций подтверждены на живом файле (Водопровод/Газопровод/Кабель.../ЛЭП/Теплосеть и т.д., конвенция `<источник>|<категория>`) — см. `docs/DATA_STRUCTURE.md` §9.
- [x] **Точный текст норм получен и внесён**: пользователь прислал 10 нормативных актов, все обработаны, ключевая таблица отступов дерева/кустарника от коммуникаций (743-ПП, Таблица 3.6.1) внесена в `docs/NORMATIVE_REFERENCES.md` со статусом `verified` + сиды `data/reference/offset_norms.yaml`, `data/reference/invasive_species.json` (369-ПП, полный перечень 21 инвазивного вида). Резолвлена путаница с СП 42.13330.2016 (там нет норм для растений, только для сетей — Таблицы 12.5/12.6, тоже сохранены).
- [ ] Структурировать `raw_species_directory_strings.txt` по колонкам (идентификатор/жизненная форма/порода/код ценности/базовая стоимость) в рабочий `data/reference/plant_catalog.*`.
- [x] Разобрать `Шаблоны значков.dwg` (библиотека условных обозначений) — сделано и пошло дальше исходной формулировки: не просто разобрано, а 3 универсальных блока (`Дерево_Л_С`/`Дерево_Х_С`/`Куст_Л_С`) реально интегрированы в DXF Export Agent (2026-09-24, см. пункт «Блоки-значки организаторов» ниже, `docs/LIMITATIONS.md`).
- [ ] Проверить структуру `Ассортиментной ведомости` (категории: лиственные/хвойные деревья, лиственные кустарники, многолетники, луковичные) — использовать как таксономию типов посадки вместо укрупнённого «дерево/куст/трава» из ТЗ.
- [ ] Разобрать, как читаются реальные координаты геодезических опорных точек с чертежа (текст рядом со слоями «Геодезические пункты»/«Координатные кресты») — нужно для пересчёта в реальную CRS.
- [ ] (Фаза 2) Дендрологический источник глубины/прогноза роста корней — в датасете не найден, справочник пород даёт только жизненную форму и стоимость.
- [ ] Уточнить у пользователя: сохранять ли полную распаковку датасета (23 ГБ, `data/raw/dataset_extracted/`, добавлено в `.gitignore`) локально, или после структурирования нужных фрагментов можно почистить.

## Фаза 2 — Каркас кода

- [x] Настроить окружение: `ezdxf`, `shapely`, `pyyaml`, `jinja2`, `openpyxl`, `python-docx`, `pymupdf`, `pytest` — версии зафиксированы точно в `requirements.txt`. ODA File Converter установлен как системная зависимость (не pip).
- [x] DWG→DXF Converter — `pipeline/ingest/dwg_convert.py`, обёртка над ODA File Converter CLI, с обработкой отсутствия конвертера (`ConverterNotFoundError`, не падение с невнятной ошибкой).
- [x] **DXF Ingest Agent** — `pipeline/ingest/dxf_ingest.py` + `pipeline/ingest/layer_classifier.py`. Реально проверено на живом объекте: 140447 классифицированных объектов-ограничений по 11 типам, детерминированный порядок (сортировка по handle/имени файла), честная пометка низкой уверенности для автоматически собранной границы участка (`found_low_confidence`, не выдаётся за надёжную). **Критичная находка, изменившая контракт**: контракт входа — НАБОР файлов объекта (компонентные `up`/`tp`/`kl`-файлы eTransmit-бандла), не один главный DWG — см. `docs/DATA_STRUCTURE.md` §10 (главный «Генплан»-файл ссылается на инженерные сети через нерезолвленный XREF, реальной геометрии в нём при прямом парсинге нет). Разбор дендроплана существующих насаждений — реализован как `existing_tree_preserve` (генерик-точки без вида, см. §4/§9), сопоставление с видом из перечетной ведомости — ещё не сделано (Фаза 2/Excel Ingest).
- [ ] Territory/Case Onboarding Agent (валидация комплектности под нужный сервис, реестр объектов, терпимость к неоднородным именам папок и к контракту «набор файлов, не один файл»).
- [ ] Улучшить сборку границы участка из разрозненных отрезков (сейчас `found_low_confidence` на реальных данных — см. `docs/DATA_STRUCTURE.md` §11) — либо через кадастр/ГПЗУ как внешний источник, либо через эвристику сшивки почти-смыкающихся отрезков с допуском.
- [x] **Формализован процесс тестирования** — `docs/TESTING.md`: как запускать, что значит «тест прошёл», честно про отсутствие автоматического наблюдателя за тестами, таблица покрытия.
- [ ] **Видо-специфичная агрессивность корневой системы** (см. `docs/ARCHITECTURE.md` §3.1b, `docs/OPEN_QUESTIONS.md`) — ждём источник данных от пользователя ИЛИ решение делать надбавку на общей практике уже сейчас. Не начинать реализацию до явного решения по объёму.
- [x] Написан тест, фиксирующий текущее (осознанное) ограничение «вид растения не влияет на отступ» — `tests/test_known_limitations.py` — чтобы будущее изменение логики было осознанным, а не случайным дрифтом.
- [x] **Найден и внесён основной ассортимент растений по категориям территорий** — 623-ПП, Приложение В, Таблица В.6 (74 вида, парки/скверы/улицы/дворы/спецзоны) — `data/reference/species_territory_recommendations.json`, `pipeline/catalog/species_territory_recommendations.py`. Извлечено через python-docx по реальной структуре таблицы (не по плоскому тексту — там был риск сдвига данных). Это ответ на «какой вид для какой территории», НЕ на вопрос глубины корней — тот вопрос остаётся открытым.
- [x] **Найден более новый и детальный источник ассортимента по территориям** — официальные файлы ДПиООС с mos.ru («Основной ассортимент», «Перспективные виды»), 329 видов, 8 категорий территорий (vs 5 у 623-ПП), с явной причиной ограничения по 7 сноскам и перекрёстной ссылкой на 369-ПП — `data/reference/dpioos_species_assortment.json`, см. `docs/NORMATIVE_REFERENCES.md`. Решено считать основным источником при расхождении с 623-ПП/В.6 (новее, детальнее). Код-обёртка (по аналогии с `species_territory_recommendations.py`) ещё не написан — сид готов, потребитель — нет.
- [ ] Собрать полный Plant Catalog для Placement Generator из уже найденных источников: `raw_species_directory_strings.txt` (жизненная форма/стоимость), `dpioos_species_assortment.json` (основной источник рекомендаций по территориям), `species_territory_recommendations.json` (вторичная сверка, 623-ПП), 515-ПП Таблица 4 (размер кома) — свести в один рабочий реестр вида.
- [x] Constraint/Buffer Engine — базовая версия готова и покрыта тестами (`pipeline/constraints/buffer_engine.py`, `pipeline/constraints/offset_registry.py`). Работает на верифицированных отступах 743-ПП.
- [x] Охранные зоны ЛЭП по классу напряжения (160-ПП) — `pipeline/constraints/lep_zones.py`, интегрировано в buffer_engine через `explicit_distance_m`.
- [x] Политика отступа от существующих сохраняемых насаждений (практическое допущение, не норма — согласовано с пользователем) — `pipeline/constraints/existing_vegetation.py`.
- [x] Каталог инвазивных видов (`pipeline/catalog/invasive_species.py`) — проверка по ру/лат названию, реестр 369-ПП.
- [x] **Тест на повторяемость результата** (`tests/test_reproducibility.py`) — обязательное требование пользователя к демо (жюри может попросить повторить прогон): два независимых прогона с одним входом дают геометрически идентичный результат (`shapely.equals`) и идентичный по составу и порядку список обоснований; порядок объектов на входе не влияет на итог. Итого 12/12 тестов зелёных на 2026-09-15.
- [x] **Placement Generator (baseline — MVP, детерминированные правила)** — `pipeline/placement/generator.py`. Дерево — сетка-кандидат с шагом из верифицированной 743-ПП/Таблицы 3.6.2 («групповая посадка», середина диапазона), точка принимается только строго внутри допустимой зоны. Вид — первый по алфавиту рекомендованный (ДПиООС) и не инвазивный (369-ПП); честный `no_recommended_species`, если подходящего вида нет.
  - [x] **Чек-боксы выбора видов перед автогенерацией** (2026-09-24, прямой запрос пользователя — «список растений с чек-боксами, чтобы пользователь мог сначала задать перечень растений») — закрывает пункт, ранее зафиксированный как идея без реализации в `docs/OPEN_QUESTIONS.md` (2026-09-18). Бэкенд: `pick_species_multi()` + `generate_placement(selected_species_names=...)` — несколько выбранных видов распределяются по точкам round-robin в алфавитном порядке (детерминированно, без seed); те же фильтры, что и в однвидовом режиме (рекомендован+не инвазивен — перепроверяется заново, чек-бокс не обходит 369-ПП) плюс фитосанитарный конфликт с уже выбранными видами другой жизненной формы. `PlacementResult.chosen_species_entries: list[...]` — новое поле (`chosen_species_entry` сохранён как первый элемент, обратная совместимость подтверждена отдельным тестом). Стоимость теперь считается по каждой точке отдельно (не `count × unit_cost` — в многовидовом режиме у точек разная цена). API: `POST /api/demo-objects/{id}/generate` принимает необязательный `selected_species: {"tree": [...], "shrub": [...]}`. Веб-UI: чек-бокс-список с поиском на вкладке «Автоматическая генерация» (`setupSpeciesPicker()`, переиспользует существующий `/api/species`) — список видов сбрасывается при смене категории территории (виды зависят от неё). 7 новых тестов (5 в `tests/test_placement_generator.py`, 2 в `tests/test_web_app.py`) + подтверждено вживую через Playwright (реальные клики по чек-боксам, поиск, генерация — несколько выбранных видов реально видны на маркерах результата). **Известная оговорка** (не исправлена в этом проходе): DXF-экспорт при выборе хвойное/лиственное блока-значка по-прежнему смотрит на один представительный вид, не на вид конкретной точки — см. `docs/LIMITATIONS.md`. Полный `pytest tests/ -q` — 177/177 зелёных.
- [x] **Placement Review Agent — второй режим работы** (2026-09-18, `pipeline/review/placement_review.py` + CLI `pipeline/review_run.py`) — прямое решение пользователя: человек сам предлагает вид+точку, сервис только независимо проверяет (вид/каталог, инвазивность, фитосанитарный конфликт между ЛЮБОЙ парой видов, отступы 743-ПП, густота — верхняя И нижняя граница 623-ПП/В.1) и честно объясняет нарушение со ссылкой на норму, БЕЗ автоматических исправлений. Добавлено рядом с автогенератором, не заменяет его. Проверено на реальном объекте («Песчаный переулок» — вымышленный вид + недостаточные отступы от ЛЭП/сохраняемого насаждения + заниженная густота, всё честно поймано).
  - [x] **Минимальное расстояние между точками одного вида посадки** (2026-09-24, `_check_minimum_spacing`) — реальный пробел, найденный пользователем вживую в веб-UI: два дерева, перетащенных почти в одну точку (0.54 м друг от друга), проходили проверку без единого замечания — густота считает только агрегатное число посадок на площадь (623-ПП), отступы — расстояние до коммуникаций, а расстояние между двумя КОНКРЕТНЫМИ точками одного вида посадки не проверял никто. Автогенератор эту норму (743-ПП, Таблица 3.6.2 «групповая посадка», ~6 м для дерева) и так соблюдает по построению (шаг сетки) — здесь та же норма применена как независимая геометрическая проверка пар точек одной жизненной формы (дерево с деревом, куст с кустом; дерево с кустом в одной точке — не нарушение этой нормы). 4 новых теста + подтверждено вживую через реальный API веб-сервиса на координатах из отчёта пользователя. Итого 16 тестов в `tests/test_placement_review.py`, полный набор — 170/170 зелёных.
- [x] **Веб-UI — второй способ запуска** (2026-09-18, `pipeline/web/` — FastAPI + vanilla JS без CDN/фреймворков) — прямой запрос пользователя (готовит презентацию). Живой сервис (не статика): визуализация участка (SVG прямо из реальной геометрии), два режима — автогенерация (запускает `run_service_a`) и проверка человеческого выбора через **drag-and-drop** (палитра дерево/куст → перетащить на план → всплывающий выбор вида → результат проверки красит маркеры и печатает нарушения). Работает на заранее сконвертированных демо-объектах (`data/demo_objects/`) — не на произвольной загрузке DWG (для этого отдельно — загрузка своего DWG/DXF, см. ниже). Swagger на `/docs` (отдельный критерий оценки ТЗ). Карта + реестр зданий data.mos.ru — сознательно отложены, отдельная задача. Проверено вживую через Playwright (headless Chromium): оба сценария, включая реальный drag-and-drop, работают корректно и внутри `docker compose up web`. 9 тестов (`tests/test_web_app.py`). 120/120 тестов зелёных (116+4 skip в Docker).
  - [x] **Ребрендинг «Зелёный контур» по макету пользователя** (2026-09-24) — пользователь ранее назвал интерфейс «суперскучным», затем прислал реальный макет (скриншот) с точными значениями: шрифт Involve Medium 16 pt везде, фон страницы `#DCE7BF`, шапка `#30401C`, скругление кнопок 5 px, скругление окна чертежа 10 px, пропорция окна чертежа по образцу. Логотип (`Зелёный контур.svg`, белая заливка — готов для тёмной шапки без переделки) и сам шрифт Involve (переменный, `Involve-VF.ttf`) — нашлись локально у пользователя в `Загрузки/` (не на Google Fonts — проверено, 400 «family not found»), скопированы в `pipeline/web/static/{fonts,img}/`. `@font-face` с `format("truetype-variations")`, `font-weight: 100 900` (весь диапазон переменной оси), базовый вес 500 (Medium) на `body`. **Реальный баг найден и исправлен по пути**: браузеры НЕ наследуют `font-family` для `button`/`input`/`select`/`textarea` по умолчанию — без явного `font-family: inherit` шрифт применился бы не «ко всему тексту в сервисе», как просил пользователь, а только к обычному тексту; добавлено явное правило. **Второй реальный баг**, найденный при проверке того же прохода (не связан с брендингом впрямую, задет по пути): класс `.loading-overlay` (добавлен чуть раньше в этой же сессии) задавал `display: flex` с более высокой специфичностью, чем встроенное правило браузера `[hidden]{display:none}` — при определённых условиях атрибут `hidden` переставал скрывать индикатор загрузки; добавлен явный `.loading-overlay[hidden] { display: none; }`. `<h1>🌳 Проект озеленения территории</h1>` заменён на `<img class="logo">`, `<title>` — на «Зелёный контур». Обновлён 1 тест (`test_index_page_served` в `tests/test_web_app.py`, проверял старый текст заголовка). Проверено вживую через Playwright (реальные скриншоты обеих вкладок, `getComputedStyle` на теле страницы и кнопках) — цвета/шрифт/вес/радиусы точно совпадают с запрошенными значениями. Полный `pytest tests/ -q` — 177/177.
  - [x] **Уточнение вёрстки шапки/отступов по Figma-макету** (2026-09-24, прямой запрос пользователя со скриншотами: крупный план кнопок + полный кадр Figma-файла «Фирменный стиль „Бумажная собака"») — кнопки «Загрузить свой DWG/DXF» и «Нормативная база» в шапке переделаны с прежнего тонкого `button.secondary` (обводка, иконка-эмодзи, 12px) на сплошную заливку без обводки и без иконки, крупный скруглённый прямоугольник (`header .header-btn`, radius 14px — сознательно шире общего правила «скругление кнопок 5px», т.к. пользователь указал именно на этот макет как эталон для этой пары кнопок): первая — цвет фона страницы `#DCE7BF`, вторая — белая; обе с тёмным текстом шапки. Отступы блока `.layout` (окно чертежа + правая панель) — `padding: 40px 115px 16px` (было `16px 20px`) по точным значениям пользователя. Проверено вживую скриншотами Playwright на изолированном тестовом порту 8260 (рабочий сервер пользователя на 8000 не тронут). Полный `pytest tests/ -q` — 177/177 (только CSS/HTML, тестов не добавлялось).
  - [x] **Реальный баг: select «Категория территории» растягивался почти на всю ширину шапки** (2026-09-24, найден пользователем на живом скриншоте своего запущенного сервиса) — Chromium считает ширину закрытого `<select>` по САМОМУ ШИРОКОМУ option среди ВСЕХ вариантов, а не по текущему выбранному значению; категория `magistrali` имеет формулировку почти на 150 символов («Магистрали (шоссе, проспекты, улицы с высокоскоростным движением...)») — из-за неё select растягивался на ~1400+ px, даже когда была выбрана короткая «Дворовые территории». Исправлено: `header .controls select { max-width: 340px; text-overflow: ellipsis; overflow: hidden; white-space: nowrap; }` (только в шапке — селекты в попапах вида/прайс-листа не тронуты) + `updateSelectTitle()` в `app.js` выставляет `title` на текущий выбранный option, чтобы полный текст был доступен по наведению. Проверено вживую Playwright на худшем случае (принудительно выбрана `magistrali`) — рендерится в одну строку с логотипом и кнопками, ширина зафиксирована на 340px.
  - [x] **Константа размера окна чертежа 1640×800 px + реальный баг: правая колонка «Ваши посадки» уезжала под план вместо расположения рядом** (2026-09-24, эксперимент по прямому запросу пользователя — «посмотрим как это будет выглядеть») — `#plan-svg-container-review`/`#plan-svg-container` теперь фиксированного размера 1640×800px (было `width:100%; aspect-ratio:21/9`). При проверке всплыл независимый пред­существующий баг: CSS `grid-template-columns: 1fr 340px` висел на `.layout`, а фактические колонки (`.plan-wrap`/`.side-panel`) вложены на уровень глубже — внутрь `.tab-panel`, обычного блочного `<div>` без своего flex/grid; так как видна всегда только ОДНА `.tab-panel`, вторая колонка `.layout` всегда пустовала, и сайдбар просто стоял под планом на любой ширине окна (воспроизведено и на 1920, и на 2560px — не проблема тесноты). Тот же баг объясняет пустую зелёную область справа от плана на реальном скриншоте пользователя. Исправлено переносом grid на `.tab-panel.active` (плюс сопутствующая правка print-медиа-запроса — `.tab-panel.active { display: block; }` вместо `.layout { display: block; }`). **Побочный эффект константы 1640px**: при такой ширине плана + 340px сайдбар + отступы 115px по бокам суммарная минимальная ширина контента (~2250-2400px) превышает большинство мониторов — на 1920px появляется горизонтальный скролл (~480px), на 2560px — небольшой (~160px); это ожидаемо для фиксированной ширины и требует отдельного решения пользователя (уменьшить константу или явно допустить горизонтальный скролл). Проверено вживую Playwright на 1920 и 2560px (правая колонка реально встала рядом с планом на обеих ширинах). Полный `pytest tests/ -q` — 177/177 (дважды: до и после фикса сайдбара).
  - [x] **Режим отладки вёрстки окна чертежа** (2026-09-24, прямой запрос пользователя после неудачного эксперимента с 1640×800 — «слева куча места, давай как в другом проекте — я вручную поставлю в нужное положение и скину координаты») — `pipeline/web/static/{app.js,style.css}`. Активируется только по `?debug=layout` в адресной строке (в обычном режиме сервиса ничего не создаётся и не подключается). Добавляет к окну чертежа (`#plan-svg-container-review`/`#plan-svg-container`, смотря какая вкладка активна) две ручки — «✥ двигать» (drag) и «⤡ размер» (resize за угол) — и плавающую панель в правом верхнем углу экрана с живым пересчётом координат (`left`/`top` относительно `.layout`, `width`/`height`) и кнопкой «Скопировать» (буфер обмена). Пользователь подбирает нужное положение/размер вживую и присылает финальные числа — дальше они фиксируются в CSS как обычные значения, без угадывания на глаз. Проверено вживую Playwright (реальные `mouse.down/move/up`, не синтетические события) — перетаскивание на -200/-30px и растягивание на +150/+80px точно отразились в панели координат. Полный `pytest tests/ -q` — 177/177 (без новых тестов — инструмент только для ручной интерактивной подгонки, не часть продукта).
    - [x] **Реальный баг первой версии режима отладки: двигался только чертёж, белый фон оставался на месте** (2026-09-24, найдено пользователем сразу же при первой пробе) — ручки «двигать»/«размер» обе висели на внутреннем `#plan-svg-container-review`/`#plan-svg-container`, у которого нет собственного белого фона (он у родителя `.plan-wrap`). Исправлено: «двигать» теперь на `.plan-wrap` целиком (карточка едет одним куском вместе с тулбаром и подсказкой), «размер» остаётся на самом окне чертежа и меняет только его размер — `.plan-wrap` как блочный элемент без явного `width` сам расширяется под новый размер содержимого. Разные цвета рамки (оранжевая/синяя) — что двигается / что растягивается. Проверено вживую Playwright на порту 8265 — карточка и окно чертежа сдвигаются синхронно при drag, растягивание меняет только окно, верхний левый угол карточки не сдвигается. Полный `pytest tests/ -q` — 177/177.
    - [x] **Второй реальный баг: ручка «размер» физически уезжала за край экрана на широких окнах** (2026-09-24) — ручка рисовалась в правом нижнем углу самого окна чертежа (1640px), который при обычной ширине браузера (≤1920px, минус 115px отступов `.layout` с каждой стороны) оказывался за пределами видимой области — пользователь не мог до неё дотянуться. Исправлено архитектурно: все ручки (окно чертежа — положение/размер, боковая панель — положение/размер) перенесены с самих элементов в постоянно видимую плавающую панель (`position:fixed`) как отдельные чипы; перетаскивание чипа в любую сторону мышью двигает/растягивает целевой элемент активной вкладки на ту же дельту — независимо от того, где сейчас физически находится сам элемент. Заодно добавлена боковая панель как отдельная цель (фиолетовая рамка) — прямой запрос пользователя «раз ты боковую панель сделал, теперь её тоже двигать надо».
    - [x] **Пивот: резиновая ширина + фиксированная пропорция вместо абсолютных px** (2026-09-24, по вопросу пользователя «будут ли масштабироваться под экран?») — пользователь верно распознал, что `width:1640px; height:800px` — абсолютные пиксели, не адаптирующиеся ни под один экран (та же причина горизонтального скролла из предыдущего пункта). Выбран вариант «резиновая ширина + фикс. пропорция» (из 3 предложенных через AskUserQuestion). `#plan-svg-container-review`/`#plan-svg-container` — `width:1640px; height:800px` → `width:100%; aspect-ratio: 1640/800` (2.050:1 — та же форма, но теперь адаптивная). Панель отладки обновлена — считает и показывает именно ПРОПОРЦИЮ (значение для `aspect-ratio`), а не абсолютные px, поскольку теперь в CSS переносится она. Проверено вживую Playwright на порту 8267 — план сам заполняет колонку с точным соотношением 2.050:1 без единого действия пользователя. Полный `pytest tests/ -q` — 177/177.
  - [x] **Переключатель вкладок в виде «папки» по макету пользователя** (2026-09-24, скриншот-мокап) — заменена плоская белая полоса с подчёркиванием на оливковую подложку (`#98A973`) с вкладками-«флажками» (скругление только сверху 10px): активная — сплошная белая заливка, текст `#30401C`; неактивная — прозрачная (сливается с той же оливковой подложкой по цвету, как явно указал пользователь), с тонкой белой обводкой `inset 0 0 0 1px rgba(255,255,255,.35)`, чтобы форма читалась. Отступы `.tabs` по бокам выровнены на 115px — как у `.layout` ниже, вкладки визуально стоят точно над окном чертежа. Вкладка «Проверка моего выбора» переименована в «Ручной ввод» — по тексту в макете (это содержательное переименование, не только стиль — зафиксировано отдельно на случай, если пользователь имел в виду только форму, а не текст). По прямому указанию пользователя НЕ тронуты: боковая панель («остаётся в том же формате») и легенда («остаётся как сейчас в проекте» — легенда на скриншоте-мокапе отличается от реализованной, это осознанно проигнорировано). Проверено вживую скриншотами Playwright на изолированном тестовом порту 8268 (рабочий сервер пользователя на 8000 не тронут) — обе вкладки визуально совпадают с макетом. Полный `pytest tests/ -q` — 177/177 (текстовых тестов на старое название вкладки не было, ничего не сломалось).
    - [x] **3 доработки по живому скриншоту**: вкладки приклеены к окну чертежа, форма со скосом, реальный баг с выравниванием шапки при переносе (2026-09-24) — пользователь прислал скриншот с красной пометкой на пустом промежутке в шапке. (1) «Вкладки должны быть приклеены к полю самого чертежа» — `.layout` верхний отступ `40px → 0`, `.plan-wrap` верх без скругления (`border-radius: 0 0 10px 10px`) — зазор между вкладками и карточкой теперь ровно 0px (подтверждено `getBoundingClientRect()`). (2) «Папки должны выглядеть иначе (правая со скосом)» — `.tab-btn` со скруглённых уголков на `clip-path: polygon(...)` — прямой левый край, скошенный правый (уже наверху, шире внизу), настоящая форма бумажного ярлычка вместо `border-radius`. (3) **Реальный найденный баг**: при переносе шапки в 2 строки (узкий экран/масштаб) `.controls` (второй элемент) с `margin-left:auto` продолжал прижиматься вправо ДАЖЕ будучи единственным элементом на своей строке — оставлял пустой промежуток слева, который пользователь обвёл красным на скриншоте. Исправлено: `margin-left:auto` убран, вместо него `header { justify-content: space-between; }` — по спецификации flexbox `space-between` на строке из одного элемента эквивалентно `flex-start`, поэтому `.controls` теперь встаёт строго вровень с лого выше (подтверждено: `logo.left === controls.left` на узком окне 700px). Проверено вживую Playwright на изолированном порту 8269 (порт 8000 не тронут). Полный `pytest tests/ -q` — 177/177.
    - [x] **Оливковая подложка под строкой вкладок убрана совсем** (2026-09-24, прямой запрос — «убери совсем, она не нужна») — `.tabs { background: #98a973; }` удалено, строка вкладок теперь прозрачная (виден обычный фон страницы `#DCE7BF`). Цвет `#98A973` остался только заливкой САМОЙ неактивной вкладки (`.tab-btn` вместо `background: transparent`), поэтому вкладка по-прежнему видна как отдельный «ярлычок», просто без сплошной цветной полосы вокруг. Убрана и лёгкая обводка неактивной вкладки — она была нужна только чтобы отличать вкладку от одинаковой по цвету подложки, теперь подложки нет и обводка не нужна. Проверено вживую: `getComputedStyle` подтвердил `.tabs` фон `rgba(0,0,0,0)`, заливка неактивной вкладки `rgb(152,169,115)` = `#98a973` сохранена. Полный `pytest tests/ -q` — 177/177.
    - [x] **Кнопки «Сгенерировать план» и «Проверить» перекрашены в цвет шапки** (2026-09-24, прямой запрос пользователя) — `#review-btn, #generate-btn, #generate-btn-2 { background: var(--header-bg); }` (было `var(--accent)` через общий `button.primary`). Специально по id, а не правкой самого `button.primary` — этот общий класс используют ещё 2 не упомянутые пользователем кнопки («Загрузить и обработать», «Применить отмеченное»), их цвет не менялся. Проверено вживую Playwright на изолированном порту 8271 — `getComputedStyle` всех трёх кнопок даёт `rgb(48,64,28)` = `#30401c`.
    - [x] **Высота кнопок «Загрузить свой DWG/DXF»/«Нормативная база» выровнена по select'у рядом** (2026-09-24, прямой запрос пользователя) — было 49px (padding 14px сверху/снизу) против 35px у `#category-select`; вертикальный padding `.header-btn` уменьшен до 7px — итоговая высота обеих кнопок и select'а теперь точно 35px (`getBoundingClientRect()` подтвердил). Горизонтальный padding и всё остальное не тронуто.
    - [x] **Тулбар плана — единая высота 35px, скругление 5px везде, «Печать»+чекбокс сдвинуты вправо** (2026-09-24, прямой запрос пользователя) — `.palette-item` (Дерево/Кустарник) `padding: 8px 12px` → `3px 12px` (было 45px высотой, стало 35px, вровень с select); `#print-review-btn`/`#print-generate-btn` `padding: 4px 8px` (общий `button.secondary`) переопределён точечно на `8.5px 10px` (было 26px, стало 35px) — общий `button.secondary` не тронут (используется ещё в попапах). Скругление: `.palette-item` `8px → 5px`, `.header-btn` `14px → 5px` — единое 5px у всех иконок/кнопок шапки и тулбара (`button.secondary`/`button.primary` уже были 5px). Порядок сдвига вправо: `margin-left: auto` перенесён с `.legend` на кнопку «Печать» — теперь «Печать» + чекбокс «Скрыть рекомендации» + легенда идут единым кластером у правого края тулбара, палитра «Дерево/Кустарник» осталась слева. Проверено вживую Playwright на изолированном порту 8273 (порт 8000 не тронут) — все три элемента (select/палитра/печать) дают ровно `35px` по `getBoundingClientRect()`, скриншоты обеих вкладок подтвердили сдвиг вправо.
    - [x] **Текст кнопок «Загрузить свой DWG/DXF»/«Нормативная база» — сначала 16pt, затем сразу переигран на размер select'а рядом** (2026-09-24) — сначала пользователь подтвердил через AskUserQuestion «именно 16pt (≈21px)» вместо прежних 16px; кнопка «Нормативная база» после этого переносилась на новую строку в шапке (21px не поместился), и пользователь тут же попросил вместо этого взять размер как у select «Категория территории» рядом. Итог: `font-size: 14px` (значение `select`), `padding` вертикальный подобран заново под новый размер шрифта — `8px 28px` — высота кнопок снова точно `35px`, как и `#category-select` (`getBoundingClientRect()` подтвердил). Проверено вживую Playwright на изолированном порту 8275 (порт 8000 не тронут) — обе кнопки в одну строку с select, без переноса.
    - [x] **Скругление select'ов «Объект»/«Категория территории» — тоже 5px** (2026-09-24, прямой запрос — «на этих плашках тоже поставь скругление 5») — общее правило `select { border-radius: 6px }` → `5px`, довершает единое скругление 5px по всему интерфейсу (кнопки, палитра, header-btn — уже были 5px, select оставался единственным исключением на 6px). Проверено вживую Playwright на изолированном порту 8276 (порт 8000 не тронут) — `getComputedStyle` подтвердил `5px`.
    - [x] **Верхние углы окна чертежа и «папок»-вкладок — тоже 5px** (2026-09-24, прямой запрос) — `.plan-wrap` (было `border-radius: 0 0 10px 10px` — верх квадратный, чтобы примкнуть к вкладке) → `5px 5px 10px 10px` (верх мягко скруглён, низ не тронут). `.tab-btn` — форма всё ещё через `clip-path` (полноценная дуга border-radius технически несовместима с clip-path — он рисует форму заново, игнорируя border-radius), поэтому в оба верхних угла полигона добавлена фаска 5px (`polygon(0 5px, 5px 0, calc(100% - 19px) 0, calc(100% - 14px) 5px, 100% 100%, 0 100%)`) — на таком масштабе визуально не отличимо от истинного скругления. Проверено вживую скриншотом на изолированном порту 8277 (порт 8000 не тронут).
    - [x] **Скругление окна чертежа возвращено к единому 10px, скругление «папок» переделано на настоящую дугу** (2026-09-24, два быстрых уточнения подряд) — сначала пользователь попросил «одинаковое скругление у папок 5, у окна чертежа 10» (смешанное 5px/10px по верхним/нижним углам `.plan-wrap` из прошлого пункта отменено — теперь просто `border-radius: 10px` целиком). Затем — «у папок обрублены края сверху»: фаска 5px, добавленная в прошлом пункте вместо истинного скругления (технически неизбежная замена, т.к. border-radius несовместим с clip-path), на практике смотрелась грубым сколом, а не скруглением. Заменена на честную дугу — верхний левый угол (единственный настоящий угол 90° в форме «папки») аппроксимирован 4 отрезками по окружности радиуса 5px, читается как реальное скругление. Верхняя точка скоса справа (вершина между горизонталью и наклонной гранью, не угол 90°) оставлена острой — фаска там смотрелась ещё хуже, а сама вершина малозаметна на готовой кнопке. Проверено вживую (крупный план угла на изолированном порту 8278, порт 8000 не тронут) — видна настоящая плавная дуга, а не срез.
    - [x] **«Загрузить и обработать» тоже перекрашена в цвет шапки** (2026-09-24, прямой запрос) — `#upload-submit-btn` добавлена в список `#review-btn, #generate-btn, #generate-btn-2 { background: var(--header-bg) }` (был сознательно оставлен зелёным раньше — теперь пользователь явно попросил и его). `#price-list-apply-btn` не упоминалась, не тронута. Проверено вживую Playwright на изолированном порту 8279 (порт 8000 не тронут) — `getComputedStyle` даёт `rgb(48,64,28)`.
    - [x] **БАГ: категория «Парки» — точки-кустарники визуально «за пределами разрешённой зоны»** (2026-09-24, жалоба пользователя со скриншотом) — реальное размещение верно (`allowed.contains(pt)` в `generator.py`), баг в отрисовке: `drawBasePlan()` рисовал только `allowed_zone.tree`, а `allowed_zone.shrub` (почти всегда шире — у кустарника отступы 743-ПП меньше/не заданы) не рисовался вовсе, хотя CSS-переменная `--allowed-shrub` под это уже была объявлена, но не использовалась. На «Песчаном переулке»: 357.75 м² (кустарник) vs 219.14 м² (дерево). У «Парков» одна из самых высоких норм густоты кустарника (623-ПП: 800-1000 шт/га) — расхождение стало массово заметным именно там. Исправлено: `app.js` теперь рисует зону кустарника (`var(--allowed-shrub)`) под зоной дерева; легенды обеих вкладок (`index.html`) дополнены строкой «допустимо (кустарник)». Проверено вживую Playwright на изолированном порту 8280 (порт 8000, PID 14052, не тронут) — категория «Парки», генерация 45/45 точек, все точки-кустарники визуально внутри новой зоны. Подробности — `docs/BUGS.md`, `docs/SESSION_LOG.md`.
  - [x] **Ещё 2 демо-объекта добавлены** (2026-09-24, прямой запрос пользователя — «посмотреть как работает выбор») — «Багрицкого улица» (88 739,5 м², сверено с цензом `docs/DATA_STRUCTURE.md` §11.2) и «Макеева С. ул» (93 155,96 м², тоже сверено) — DWG сконвертированы через тот же `pipeline/ingest/dwg_convert.py`, площадь и `site_boundary_status=found` подтверждены `ingest_dxf_files()` до регистрации в `pipeline/web/demo_objects.py`. `data/demo_objects/` — 129 МБ на 3 объекта (было ~14 МБ на 1). Третий кандидат («Грузинская М ул») намеренно НЕ добавлен — агент ОТК 6.1a честно вернул `implausible` (ratio 0.00064, реальный выброс в исходных DWG раздувает bbox коммуникаций до ~61 км²) — тот же класс проблемы, что уже припаркован для «Кустанайской улицы» (§11.5), не форсируется без ручной проверки исходника. Полный прогон `run_service_a` на обоих новых объектах — все 4 применимые проверки ОТК `ok`. Полный `pytest tests/ -q` — 170/170 зелёных.
- [x] **Оценочная стоимость посадок по категориям** (2026-09-16, по прямому запросу пользователя) — `data/reference/planting_cost_estimates.yaml` (категории: хвойное/лиственное × дерево/кустарник × размер, цены с реальных прайс-листов питомников, редактируемы человеком — обязательное условие) + `pipeline/catalog/planting_cost.py`. `estimated_cost_rub` на каждый вид посадки и итог в CLI/отчёте, сравнение со справочным целевым диапазоном (10-25 млн руб/га, устное упоминание организаторов) — диапазон НЕ ограничивает густоту (проверено: реальный ограничитель — норма плотности 623-ПП, см. пункт ниже).
- [x] **Норма плотности + кластеризация кустарника небольшими группами** (2026-09-16, по прямому запросу пользователя — «зачем заполнять всю территорию») — `data/reference/planting_density_per_hectare.yaml` (623-ПП/МГСН 1.02-02, Таблица В.1, шт/га по категории территории) + `pipeline/placement/density.py` + `ClusterConfig`/`_cluster_candidates()` в `generator.py`: кустарник размещается небольшими компактными группами (СП82.13330.2016 п.9.30), число которых подобрано под норму плотности, а не сплошной сеткой по всей территории. Реальный объект: было 292932 куста на 49,4 га, норма ограничивает разумным числом. Размер группы (7 шт.) — честно задокументированное инженерное допущение, не норма.
- [x] **Фитосанитарная совместимость дерева и кустарника (сноска [6] ДПиООС)** (2026-09-16, по инициативе пользователя) — можжевельник (переносчик ржавчинных грибов) исключается из выбора при конфликте с уже выбранной яблоней/грушей/айвой на том же участке, обоснование видно в отчёте (`species_conflict_note`). Единственное правило совместимости с верифицированным источником — более широкая аллелопатия/ярусность/освещённость по-прежнему не реализованы (нет источника), см. `docs/ARCHITECTURE.md` §3.1c.
- [x] **Честное обоснование выбора вида среди равно допустимых** (2026-09-16, по прямому запросу пользователя — «алгоритм должен объяснить, почему именно это растение») — `PlacementResult.eligible_alternatives` перечисляет все остальные равно допустимые виды, `TIE_BREAK_EXPLANATION` явно объясняет, что алфавитный порядок — детерминированный тай-брейк, а не содержательное предпочтение (источник не даёт критерия различать равно допустимые виды). Видно в JSON-отчёте и в CLI.
- [x] **Аудит-лог веб-UI** (2026-09-19, `pipeline/web/audit_log.py`, прямой запрос пользователя — «потом могут спросить, что сервис этого не порекомендовал») — каждый вызов `/generate` и `/review` пишет ПОЛНЫЙ, нетронутый ответ в `data/web_audit/<UTC-дата>.jsonl`, независимо от того, что пользователь скроет на экране (см. пункт ниже). Не публикуется в git, персистентно переживает перезапуск контейнера (том `./data:/app/data` уже смонтирован). 2 теста.
- [x] **Печать + «Скрыть рекомендации сервиса»** (2026-09-19, `pipeline/web/static/`) — кнопка печати текущей вкладки (`@media print` скрывает шапку/вкладки/тулбар), переключатель прячет текстовые блоки-рекомендации (`.service-recommendation`), оставляя план и выбор пользователя — решение о посадке остаётся за пользователем (согласовано: цвет маркеров не трогается). Проверено вживую через Playwright (обычный вид + print-эмуляция).
- [x] **Защита загрузки нормативной базы** (2026-09-19, `pipeline/common/schema_validation.py`, прямой запрос пользователя — обновление норм БЕЗ правки кода) — все 9 реестров (`data/reference/*.json|yaml`) валидируют структуру верхнего уровня при загрузке (понятная `DataValidationError` вместо случайного `KeyError`), обновление — ручная замена файла (уже сегодня подхватывается без перезапуска, реестры не кэшируются). `GET /api/data-sources` + кнопка «ℹ️ Нормативная база» в UI честно показывают дату файла на диске для каждого справочника. Регламент замены — `docs/DATA_UPDATE_GUIDE.md`. 5 новых тестов + прогон полного набора против боевых данных (не сломано).
- [x] **Порядок вкладок веб-UI** (2026-09-19, прямой запрос пользователя) — «Проверка моего выбора» теперь первая/активная вкладка по умолчанию, «Автоматическая генерация» вторая.
- [x] **Индикатор загрузки при смене объекта/категории + вторая кнопка «Сгенерировать план»** (2026-09-24, прямой запрос пользователя — «нужно окно загрузки, чтобы показать, что сервис не завис» + «разместить в окне „Результат" кнопку „Сгенерировать план"») — `pipeline/web/static/{index.html,app.js,style.css}`. `loadGeometry()` (вызывается при смене объекта/категории на ОБЕИХ вкладках сразу) теперь показывает полупрозрачный оверлей со спиннером поверх плана на время запроса — реальная находка при проверке: переключение на объект побольше («Багрицкого улица») заняло больше 15 с в одном из прогонов, то есть индикатор не косметика, а реально нужен. Кнопка `#generate-btn-2` — точная копия существующей `#generate-btn` (тот же обработчик `runGenerate`, переключаются вместе в состояние «Генерирую...»), размещена в блоке «Результат» рядом с итогом генерации. Проверено вживую через Playwright: оверлей появляется сразу после смены объекта и скрывается по завершении загрузки; клик по второй кнопке запускает генерацию, обе кнопки блокируются вместе, маркеры реально появляются на плане. Полный `pytest tests/ -q` — 177/177 (без новых тестов — фронтенд, верификация через реальный браузер).
- [x] **Загрузка своего DWG/DXF через веб-UI** (2026-09-19, `pipeline/web/uploads.py`, прямой запрос пользователя — «а если на презентации дадут новый DWG файл?») — кнопка «📤 Загрузить свой DWG/DXF» в шапке, принимает несколько файлов сразу (переиспользует файл-агностичный `ingest_dxf_files()`), DWG конвертируется через существующий `pipeline/ingest/dwg_convert.py`. Обработка синхронная, со спиннером (согласовано: без очереди задач — на крупных объектах честно предупреждает про несколько минут ожидания). Загруженный объект появляется в общем списке объектов наравне с демо-объектами и работает в обоих режимах без отличий (`_resolve_object()` в `pipeline/web/app.py` унифицирует оба источника). Реестр загрузок — только в памяти процесса (не переживает перезапуск, это ожидаемо). Ошибки конвертера/отсутствия границы участка — понятные 4xx/503, не 500. Залогировано в аудит-лог (`kind="upload"`). 3 новых теста + сквозная проверка через Playwright (загрузка → генерация на загруженном объекте — 18/18 точек размещено).
- [x] **Обновление цен из прайс-листа питомника** (2026-09-19/20, `pipeline/catalog/price_list_ingest.py`, прямой запрос пользователя — «никто так цены обновлять не будет! нужна загрузка для прайс-листа с распознаванием растений») — разбор .xlsx/.xls/.docx/.pdf (`openpyxl`/`python-docx`/`pymupdf`, уже были в зависимостях), распознавание категории строки в 2 шага: точный лукап по каталогу ДПиООС, при неудаче — fallback по родовым словам (хвойность/дерево/кустарник), **выведенным из самого каталога, а не заданным вручную**. Строка, которую не удалось классифицировать НИ ОДНИМ способом, не отбрасывается — попадает в предпросмотр как `unclassified`. Если категории ещё нет в файле — предлагается НОВАЯ (не подгоняется под существующую). Предпросмотр (`POST /api/price-list/preview`) ничего не пишет на диск; применение (`POST /api/price-list/apply`) — по решению пользователя «сразу на сервере, с бэкапом» — обновляет `data/reference/planting_cost_estimates.yaml` напрямую, предварительно сохранив бэкап (`data/reference/backups/`, не более 2, имя по дате). Живая проверка через Playwright нашла и позволила исправить реальный баг классификации (см. `docs/SESSION_LOG.md`, 2026-09-20) — обычные лиственные породы без слова «дерево» в названии («Липа мелколистная» и т.п.) неверно попадали в unclassified. 14 новых тестов (10 юнит + 4 API), полный набор — 148/148 зелёных.
- [x] **Оценочная стоимость посадок при ручном вводе** (2026-09-20, `pipeline/review/placement_review.py`, прямой запрос пользователя — «отсутствует примерный подсчёт стоимости посаженных растений при ручном вводе») — `PlacementReviewReport.total_estimated_cost_rub`/`cost_unknown_count`: та же логика оценки, что и в автогенерации (`pipeline/catalog/planting_cost.py::default_cost_for`), но по КАЖДОЙ точке отдельно (у ручного ввода у каждой точки может быть свой вид, не один общий на весь участок). Вид, не найденный в каталоге (уже отдельно диагностируется как `unknown_species`), честно исключается из суммы, не додумывается. Показывается в веб-UI на вкладке «Проверка моего выбора» тем же способом, что и на вкладке автогенерации. 5 новых тестов (3 в `test_placement_review.py`, 2 в `test_web_app.py`).
- [x] **Drag-and-drop — видимый кружок вместо нативного drag-призрака** (2026-09-20, `pipeline/web/static/app.js`, повторный прямой запрос пользователя после первой попытки через `dataTransfer.setDragImage`) — первая версия не была видна вживую (рендер OS-уровня негарантирован для нативного HTML5 drag-image). Заменено на полностью самодельный drag через `mousedown`/`mousemove`/`mouseup`: реальный `<canvas>`-кружок добавляется в `document.body` и двигается вслед за курсором на каждый `mousemove` — гарантированно видим в любом браузере. `draggable="true"` убран из палитры (`index.html`), чтобы браузер не пытался параллельно начать нативный drag. Проверено вживую через Playwright реальными событиями мыши (`page.mouse.down/move/up`, не синтетические `DragEvent`): кружок появляется в DOM сразу на mousedown, координаты (`left`/`top`) корректно следуют за курсором на промежуточном шаге, исчезает и открывает попап выбора вида на mouseup внутри плана.
  - [x] **Редактирование уже поставленных точек: перетаскивание и удаление кликом** (2026-09-24, `attachProposalMarkerInteractions()`) — прямое замечание пользователя: «почему нельзя подвинуть уже перемещенные на карту объекты или редактировать (удалить, например)?» — раньше удалить точку можно было только через крестик в списке «Ваши посадки», а сдвинуть уже поставленную точку было вообще нельзя. Теперь клик по маркеру на карте БЕЗ движения мыши (порог 3 px) удаляет точку — то же действие, что и крестик в списке, но не нужно искать нужную строку среди сотен; перетаскивание (движение выше порога) — сдвигает точку на новую позицию, координаты в `state.proposals`/списке обновляются. `e.stopPropagation()` на mousedown маркера — чтобы одновременно не запускалось панорамирование фона (`container.mousedown` в `setupZoomPan`). Проверено вживую через Playwright реальными событиями мыши на объекте «Песчаный переулок»: перетаскивание меняет координаты в списке (точка не пропадает), клик без движения убирает точку и с карты, и из списка. Автогенерация (read-only результат алгоритма) не затронута — интерактивность только у маркеров вкладки «Проверка моего выбора». Полный `pytest tests/ -q` — 170/170 (изменения только во фронтенд-коде, бэкенд не тронут).
  - [x] **Подсветка ошибок на карте усилена + переход к точке кликом** (2026-09-24) — прямое замечание пользователя: «растений могут быть сотни, как узнать, где он накосячил?». Базовая покраска маркера в красный (`marker-issue`, если `planting_id` точки есть среди `issues`) уже существовала — усилено: (1) проблемные пункты в списке «Ваши посадки» теперь получают ⚠️ и подсветку фона (`renderProposalList()` сверяет с `state.lastReview`, раньше список не обновлялся после проверки вообще — `runReview()` не вызывал `renderProposalList()`, реальный недостающий вызов); (2) КАЖДОЕ замечание, привязанное к конкретной точке (`planting_id`, не густота — та не привязана к одной точке по своей природе), кликабельно — клик приближает карту (`jumpToPointOnMap()`, сохраняет текущее соотношение сторон, зум до ~12% от исходной ширины участка) прямо к этой точке и коротко подсвечивает её маркер пульсацией (`marker-flash`, CSS-анимация); (3) клик по помеченной ⚠️ строке списка делает то же самое — не нужно искать проблемную точку глазами среди сотен на общем виде. Проверено вживую через Playwright: 3 точки (2 намеренно слишком близко — `spacing_too_close`), после проверки все проблемные строки списка помечены, клик по замечанию и клик по строке списка оба уменьшают `viewBox.width` (реальное приближение), маркер получает класс `marker-flash`, клик по строке списка НЕ удаляет точку (отдельный обработчик, не пересекается с click-to-delete на самом маркере). Полный `pytest tests/ -q` — 177/177 (без новых тестов — верификация через реальный браузер, изменения только во фронтенде).

Итог по чек-листу из 4 пунктов пользователя (2026-09-24): все 4 закрыты — редактирование точек на карте, объяснение категории территории, чек-боксы видов, подсветка ошибок.
- [ ] **Агент ОТК, полная версия** — все пять проверок (6.1–6.5 в `AGENTS_PLAN.md`).
  - [x] **6.1a** (2026-09-16, `pipeline/otk/boundary_plausibility.py`) — независимая проверка правдоподобия площади границы относительно охвата коммуникаций, найдена и обоснована реальным багом (см. `docs/DATA_STRUCTURE.md` §11.3).
  - [x] **6.1 (продолжение) Независимая сверка отступов** (2026-09-17, `pipeline/otk/offset_compliance.py`) — для каждой точки посадки пересчитывает фактическое расстояние до сырой геометрии каждого объекта-ограничения (`Point.distance`) и сравнивает с требуемым отступом (`OffsetRegistry`), не используя вычисленный `ConstraintMapResult.allowed_zone`; пространственный индекс STRtree (`predicate="dwithin"`) — производительность на объектах с десятками тысяч объектов-ограничений. 7 тестов; проверено на реальных объектах — «Песчаный переулок» (tree `ok`/4, shrub `ok`/14) и «Олимпийская деревня» (tree `ok`/427, shrub `ok`/1113), 0 нарушений на обоих.
  - [x] **6.2 Целостность слоёв** (2026-09-17, `pipeline/otk/layer_integrity.py`) — независимая проверка (открывает оба DXF заново, не переиспользует `dxf_export.py`): сравнение по DXF handle, каждая исходная сущность обязана присутствовать неизменной, весь новый контент — только в слоях `GREEN_AI_*`. По пути найдены и обработаны 4 реальных безобидных побочных эффекта round-trip через ezdxf (`LWPOLYLINE.const_width=0.0`, `INSERT.xscale/yscale/zscale=1.0`, `ELLIPSE.extrusion=(0,0,1)`, `MTEXT.flow_direction=1` пропадают при перезаписи) — узкий явный allowlist, не общий механизм. 10 тестов; проверено на реальных объектах — «Песчаный переулок» (`ok`, 0 issues) и «Олимпийская деревня» (217417 сущностей, `ok`, 0 issues, 47,6 с).
  - [x] **6.3 Анти-галлюцинационная сверка обоснований** (2026-09-17, `pipeline/otk/citation_integrity.py`) — на уже готовом JSON-отчёте пересчитывает каждую цитату заново из свежих `OffsetRegistry`/`DpioosSpeciesCatalog`/`LepZoneRegistry` и сравнивает буквально; ключевой инвариант — неверифицированная строка обязана нести честный плейсхолдер `"[требует верификации]"`, а не подставленный текст. 8 тестов; проверено на реальных объектах — «Песчаный переулок» (`ok`, 3877 цитат) и «Олимпийская деревня» (`ok`, 292 003 цитаты, 0,83 с), 0 нарушений на обоих.
  - [x] **6.4 Регрессия на эталонных данных** (2026-09-17, `tests/test_service_a_regression.py`) — полный прогон Service A (DWG→DXF→Ingest→...→Export→все проверки ОТК) на подтверждённом реальном объекте («Песчаный переулок»), skip если нет ODA File Converter/датасета. Повторяемость (2 прогона, DXF+отчёт идентичны) + golden baseline (площадь границы, число посадок, все проверки ОТК = `ok`). Реальная находка по пути: ezdxf штампует `$TDCREATE/$TDUPDATE/$VERSIONGUID` + служебную запись версии заново при каждом `saveas()` (метаданные момента сохранения, не решения пайплайна) — узкая нормализация перед сравнением, тот же принцип, что и allowlist в 6.2; не затрагивает более ранние результаты 6.2 (заголовок DXF вне области `check_layer_integrity()`). Автоматизирует пункт «Повторяемость на реальном объекте» ниже.
  - [ ] **6.5 Достаточность данных — не применяется** — 6.5 по определению относилась к Service B (Ad-hoc Ingest — Excel/PDF/DOCX), который по решению пользователя (2026-09-24) окончательно исключён из объёма проекта (см. `docs/OPEN_QUESTIONS.md`). Проверять нечего — не пункт на будущее, а закрытый вопрос. Следующий шаг после этого пункта по списку пользователя — объекты датасета без подтверждённой границы участка (было 13, после 2026-09-17 — 11, см. ниже).
  - [x] **CLI-видимость 6.1/6.1a исправлена** (2026-09-24, `pipeline/run.py`) — прямой запрос пользователя: «жюри наверняка ждёт все 5 проверок, надо быть уверенным, что они все проходят». Реальная находка: 6.2 и 6.3 всегда печатали в CLI явный вердикт (`ok`/`violation`), а 6.1 и 6.1a печатали строку ТОЛЬКО при нарушении — на чистом объекте жюри увидело бы в терминале явные `ok` только для 6.2/6.3, а для 6.1/6.1a вообще ничего (хотя внутри они реально выполнялись и попадали в JSON-отчёт с явным `verdict` уже и раньше). Исправлено: теперь все 4 применимых к Service A проверки (6.1a, 6.1×2 — дерево/кустарник отдельно, 6.2, 6.3) печатают явный вердикт при КАЖДОМ прогоне, не только при проблеме. Проверено на реальном объекте («Песчаный переулок») — все 4 строки показывают `ok`. Полный `pytest tests/ -q` — 162/162 зелёных (без изменений в логике, только видимость).
  - [x] Оба артефакта-визуализации (Олимпийская деревня, Песчаный переулок) дополнены слоем зданий (реконструкция из отрезков стен) — раньше отсутствовал.
- [x] **DXF Export Agent** — `pipeline/export/dxf_export.py`. Новые слои (`GREEN_AI_TREE_PROPOSED`/`_REJECTED`, `GREEN_AI_SHRUB_PROPOSED`/`_REJECTED`) добавляются в НОВЫЙ файл, исходный DXF не перезаписывается (проверено тестом + вручную на реальном объекте). Работает БЕЗ ожидания протокола ОТК (полного агента ОТК ещё нет) — упрощение, зафиксировано как отличие от целевой схемы AGENTS_PLAN.md.
  - [x] **Блоки-значки организаторов из «Шаблоны значков.dwg» подтянуты для предложенных деревьев** (2026-09-24) — по прямому указанию `docs/DATA_STRUCTURE.md` §8. Извлечены 3 универсальных шаблонных блока (`Дерево_Л_С`, `Дерево_Х_С`, `Куст_Л_С`) в `data/reference/planting_symbols.dxf` (72 КБ, через `ezdxf.addons.importer.Importer`) из образцового объекта организаторов (~130 блоков, в основном видо-специфичных — не тянем всё). Для ПРЕДЛОЖЕННЫХ деревьев вставляется нужный блок (`_import_remapped_tree_block`, хвойное/лиственное — по `chosen_species_entry.life_form_group_ru`), все внутренние сущности блока перепривязаны на `GREEN_AI_TREE_PROPOSED`, исходные слои блока (`СП_дендроплан` и т.п.) удалены из результата — иначе агент ОТК 6.2 (`pipeline/otk/layer_integrity.py`) нашёл бы посторонние новые слои. ОТКЛОНЁННЫЕ деревья и кустарник (оба статуса) — без изменений: свой примитив/«пятно-масса» (вид/хвойность для отклонённых точек не определяются, а собственная кустарниковая масса уже честно отражает геометрию группы, см. `docs/LIMITATIONS.md`). 3 новых теста в `tests/test_dxf_export.py` (реальный блок вместо примитива, хвойное/лиственное дают разные блоки) + 1 новый в `tests/test_layer_integrity.py` (проверка 6.2 не находит посторонних слоёв блока). Проверено на реальном объекте («Песчаный переулок»): 4 предложенных дерева получили блок `Дерево_Л_С`, слой чистый. Полный набор — 162/162 зелёных.
  - [x] **Условные обозначения по ГОСТ 21.508-2020 перенесены из прототипа в реальный экспорт** (2026-09-23) — раньше здесь рисовались голые `POINT`+`TEXT`; теперь дерево — окружность с крестом (`_draw_tree_symbol`), кустарник — «пятно-масса» на группу с одной подписью числа экземпляров (`_draw_shrub_mass`), не точка на каждый куст. Группировка кустарника для отрисовки — простая кластеризация по расстоянию (`_cluster_shrub_points`, порог из реального `grid_spacing_m` точки, не заданная вручную константа) — не меняет модель данных генератора (`PlacementPoint` по-прежнему не хранит `cluster_id`). **Честный компромисс по трассируемости**: для деревьев id на чертеже 1:1 совпадает с id в JSON-отчёте (как раньше); для кустарника трассируемость — на уровне ГРУППЫ, не отдельного экземпляра (см. `docs/LIMITATIONS.md`) — соответствует самому ГОСТ 21.508-2020 (там кустарник тоже подписывается ведомостью по группе). 4 новых теста (геометрия символа дерева, кластер рисуется одним пятном с одной подписью, две удалённые группы не сливаются) + обновлён существующий тест под новую геометрию. Проверено на реальном объекте («Песчаный переулок»): 14 кустов корректно кластеризовались ровно в 2 группы по 7 (совпадает с реальным размером группы генератора). Полный набор — 155/155 зелёных.
- [x] **Interpretation Report Agent — MVP (только JSON)** — `pipeline/report/interpretation_report.py`. Собирает JSON: точки, вид, обоснование, применённые нормы (одним блоком на вид посадки — `applied_constraint_norms_by_kind`, НЕ на каждую точку, см. ниже про баг), сводка, `site_boundary_status` с явным предупреждением при `found_low_confidence`, вердикт агента ОТК.
  - [x] **Человекочитаемый отчёт (Markdown + HTML)** (2026-09-23/24, `pipeline/report/human_report.py`, `config/templates/human_report.{md,html}.j2`) — закрывает разрыв, найденный при анализе требований ТЗ: JSON полный, но без CAD/умения читать JSON результат не увидеть. Чистый рендерер уже посчитанного JSON-отчёта + геометрий `ConstraintMapResult` (тот же принцип «один источник истины», что и в DXF Export Agent) — схема «до/после» через `matplotlib` (по панели на вид посадки, зелёным/красным — допустимая/запрещённая зона, точки — предложено/отклонено), сводка, выбранные виды, таблица обоснований (дерево — поимённо, кустарник — сводкой по группам, тот же принцип, что и в DXF-символике), протокол ОТК (6.1a/6.1/6.2/6.3), известные упрощения, приложение по отклонённым. PDF сознательно НЕ реализован (решение пользователя, 2026-09-23) — новая тяжёлая зависимость с риском для воспроизводимости Docker-образа ради формата, который и так необязателен по букве ТЗ («на выбор команды»). Подключено к CLI необязательным флагом `--output-human-report-dir`. По пути найден и исправлен реальный баг вёрстки Markdown-шаблона (`trim_blocks` съедал перенос строки после `{% if %}`/`{% endif %}`, из-за чего пункты списка «Выбранные виды» слипались в один абзац) — исправлено явными `{{ "\n" }}`-вставками вместо переносов, зависящих от обрезки блочных тегов. 4 новых теста + визуальная проверка на реальном объекте («Песчаный переулок», скриншот HTML через Playwright). Полный набор — 159/159 зелёных.
- [x] **Визуальная план-схема (прототип, вне пайплайна)** — интерактивный HTML/SVG-план (артефакт, не файл в репозитории) на реальных данных «Олимпийской деревни»: граница, охранные зоны, деревья/группы кустарника, ведомость. Оформление приведено в соответствие с реальными условными обозначениями **ГОСТ 21.508-2020** (раздел 10, Приложения П/Т — прочитан по существу 2026-09-16): дерево — «кружок с крестиком», кустарник — пятно-масса с одной подписью на группу (не точка на экземпляр), обязательная ведомость (позиция/порода/количество). Не встроено в `pipeline/report/interpretation_report.py` как автоматический артефакт прогона — сделано вручную по запросу, требует отдельного решения, встраивать ли генерацию такого плана в пайплайн.
- [x] **Реальная геопривязка «Олимпийской деревни» (WGS84)** (2026-09-16) — построена и подтверждена независимо: контуры зданий из DXF (`shapely.ops.polygonize` по отрезкам стен) сопоставлены с реальными полигонами домов «Олимпийская Деревня» из открытых данных data.mos.ru (набор 60562), преобразование (поворот+сдвиг) уточнено методом наименьших квадратов по 9 зданиям — невязка ≤4,9 м. `data/reference/mos_60562_olimpiyskaya_derevnya.json` (сырые данные), `data/reference/olimpiyskaya_derevnya_geo_transform.json` (калибровка — только для этого объекта). Визуализация показывает реальные широту/долготу. Не встроено в основной пайплайн `pipeline/run.py` — только в прототип-визуализацию; для других объектов датасета потребует повторения процесса.
- [x] **Второй реальный объект протестирован — «Песчаный переулок»** (2026-09-17) — выявлена и исправлена ошибка выбора файлов бандла (несколько пронумерованных наборов `output_N`, взят неверный на первой попытке — см. `docs/DATA_STRUCTURE.md`). Итог на верном наборе: 4 дерева, 14 кустов (2 группы) на 5 га — допустимая зона всего 0,44% участка из-за плотной упаковки 9 типов коммуникаций (не из-за существующих деревьев — проверено отдельно). Геопривязка тем же методом, что и «Олимпийская деревня», честно НЕ подтверждена (недостаточно значимых зданий для надёжного сопоставления — 5 против 14) — не добавлена в визуализацию, граница применимости метода задокументирована в `docs/OPEN_QUESTIONS.md`.
- [x] **Слой зданий в визуализации** (2026-09-17, по прямому вопросу пользователя «а где дома?») — добавлен в оба прототипа-артефакта (реконструкция из отрезков стен, `shapely.ops.polygonize`), раньше отсутствовал в визуализации, хотя контуры уже вычислялись для геопривязки.
- [x] **Исправлен критичный баг масштабирования отчёта (2026-09-16)** — на реальном объекте (140447 норм × сотни точек) отчёт весил 168 ГБ и валил прогон по нехватке места на диске, потому что применённые нормы копировались в КАЖДУЮ точку вместо хранения один раз на вид посадки. Исправлено (`schema_version` → `1.1-mvp`), после фикса тот же объект — 414 МБ. Регрессионный тест на масштаб добавлен (`tests/test_interpretation_report.py`).
- [x] **CLI точка входа** — `python -m pipeline.run --input ... --base-dxf ... --territory-category ... --output-dxf ... --output-report ...`.
- [x] **Сквозной прогон подтверждён на реальном объекте** («Олимпийская деревня», 140447 объектов-ограничений, через ODA File Converter DWG→DXF) — пайплайн не падает; изначально честно возвращал 0 посадок из-за `found_low_confidence` границы участка.
- [x] **Проблема границы участка решена для 12 из 20 объектов датасета, из них 12 полностью демо-готовы** (граница + коммуникации оба подтверждены) — см. `docs/DATA_STRUCTURE.md` §11.2–11.6. Реализовано и покрыто тестами: поддержка `brd.dwg`-компонента бандла как источника границы; резолвинг границы, заданной INSERT-блоком (`_collect_insert_boundary_geometry()`); новые алиасы слоёв `"ДВ_ГП_П_Граница работ"`/`"Топо_Граница площадки"` + ~35 алиасов третьей конвенции имён слоёв (2026-09-17, `data/reference/layer_name_aliases.yaml`); **исправлен реальный баг** выбора границы при объединении полного бандла (приоритет ранга имени слоя перебивал достоверность геометрии — найдено по проверочному вопросу пользователя, воспроизведено на «Олимпийской деревне» и «Грузинской М ул», исправлено и покрыто регрессионным тестом). Полностью демо-готовые объекты: Олимпийская деревня, Песчаный переулок, Багрицкого улица, Старый Гай ул, Куликовская улица, Грузинская М ул, Макеева С. ул, 3-я Парковая, Камчатская улица, **2-я прядильная, Лодочная, Академика Понтрягина** (последние три — 2026-09-17, см. `docs/DATA_STRUCTURE.md` §11.6; «Лодочная» дополнительно проверена полным прогоном `run_service_a` — все проверки ОТК `ok`, 122 точки реально размещено). Остальные 8 (Харьковская улица, Наташинский пр-д, Харьковский проезд, Нижние Поля ул — подтверждённо не решаются на имеющихся данных; Измайловская площадь, улица Берзарина — не доисследованы; Кустанайская улица — граница есть, но 6.1a не доверяет; Фрунзенская набережная — вне контракта Service A) — не блокер, отдельная задача по мере необходимости.
- [x] **«Красные линии» — зона запрета размещения насаждений** (2026-09-17, `pipeline/constraints/red_lines_policy.py`) — решение пользователя, не норма 743-ПП (честно `verified=False` в отчёте, см. `docs/NORMATIVE_REFERENCES.md`). По пути добавлена поддержка сущности HATCH в Ingest (`pipeline/ingest/dxf_ingest.py`, `_hatch_to_geometry`) — реальный слой «Красные линии» рисуется тысячами мелких HATCH-плиток, раньше тип сущности вообще не поддерживался. 7 тестов; проверено на реальном объекте («Камчатская улица» — 4000 новых `red_lines_no_planting`-объектов, 6.1/6.3 проходят).
  - **«Кустанайская улица»** — переразобрана целиком, но НЕ добавлена в число демо-готовых: граница/коммуникации технически ингестируются (`found`, 167 950 м², 5634 объекта), но 6.1a честно возвращает `implausible` из-за 2 явно ошибочных точек-выбросов в исходных данных (~32 км от объекта) — не отфильтровано автоматически (не наше дело придумывать порог отсечения), нужна ручная проверка исходного DWG.
  - **Слой «Красные линии» найден на 19 из 20 объектов датасета** (2026-09-17, проверено по факту) — единственное исключение «Фрунзенская набережная» (вне контракта Service A, только PDF/Excel). Значит новая политика применима практически ко всему датасету, не только к отдельным проблемным объектам.
- [x] **Явное предупреждение о ЛЭП с неопределённым классом напряжения** (2026-09-17, `pipeline/run.py` + `pipeline/report/interpretation_report.py`) — по прямому требованию пользователя («такие вещи мы должны прописывать, чтобы человек понимал, в чём проблема»). Найдена реальная причина крошечной допустимой зоны на «Камчатской улице» (0,06% участка) — НЕ красные линии (с ними и без них результат одинаков), а 435 объектов ЛЭП с неопределённым напряжением, каждому из которых проставлен максимально консервативный отступ 55 м (тариф 1150 кВ, Постановление №160) — покрытие в одиночку 369% площади участка. Теперь CLI и JSON-отчёт (`lep_unknown_voltage`) явно показывают число таких ЛЭП, применённый отступ и honest-объяснение, что реальный класс напряжения почти наверняка ниже. 2 теста; проверено на реальном объекте. См. `docs/OPEN_QUESTIONS.md` — сама причина (нет данных о реальном классе напряжения) не устранена, только видимость.
- [x] **Dockerfile + docker-compose, проверка прогона в Linux-контейнере** (2026-09-18) — `Dockerfile` (`python:3.14-slim-bookworm`, GEOS не ставится системно — приносится shapely-wheel), `docker-compose.yml`, `docker/entrypoint.sh`. ODA File Converter — опционально (`docker/oda/README.md`): лицензионно закрыт EULA-формой на сайте ODA, поэтому НЕ automated в Dockerfile — пользователь кладёт AppImage вручную, дальше распаковка на этапе сборки (без `/dev/fuse` в рантайме). **Реальная находка при первой сборке**: `xvfb-run` (изначальный подход к запуску ODA-конвертера, Qt6 GUI-приложению нужен дисплей) завис намертво в контейнере — он ждёт SIGUSR1 от Xvfb как признак готовности, а в Docker (PID 1 в изолированном namespace) сигнал не доходил, `wait` блокировался навсегда. Исправлено: Xvfb запускается напрямую, готовность определяется опросом unix-сокета `/tmp/.X11-unix/X99`, без `xvfb-run` вообще. **Прогон pytest внутри собранного образа**: 107 passed, 4 skipped (честно — тесты, завязанные на ODA/датасет, которых в образе нет) за 1,8-2,9 с — через `docker run` и через `docker compose run` (пришлось явно задать `name:` в docker-compose.yml — путь проекта содержит кириллицу, Compose не может вывести имя проекта из директории по умолчанию).
- [x] **Производительность полного прогона на крупном реальном объекте** — решено как побочный эффект фикса густоты кустарника ниже: 309,7-344,7 с → **131,5 с** на «Олимпийской деревне» (2026-09-16, см. `docs/OPEN_QUESTIONS.md`) — резко упало число точек-кандидатов после перехода на кластеризацию по норме плотности.
- [x] **Шаг сетки кустарника давал нереалистичное число посадок на большом участке** — решено 2026-09-16 (см. `docs/OPEN_QUESTIONS.md`, полная история): 292 932 куста (шаг 0,3 м из нормы «групповая посадка» — для ОДНОЙ группы/изгороди, ошибочно применённый как сетка по всей территории) → **1 113 кустов** после перехода на реальную норму плотности (623-ПП, Таблица В.1, `pipeline/placement/density.py`) — целевое число считается по норме шт/га от ПЛОЩАДИ ДОПУСТИМОЙ ЗОНЫ (`allowed_zone.area`, уже за вычетом всех буферов/исключений), не от полной площади участка, что и объясняет, почему итог (1 113) на порядки меньше формального потолка нормы на весь участок (19 760-23 712 для этой категории территории на 49,4 га) — на практике под новую посадку остаётся лишь малая часть площади. Промежуточный эксперимент с организаторским бюджетом (10-25 млн руб/га) как ограничителем густоты не сработал и оставлен только как справочное сравнение стоимости: даже при старом абсурдном числе (292 932 куста × ~700 руб.) смета была ~4,1 млн руб/га — в разы МЕНЬШЕ потолка бюджета, то есть бюджет (явно покрывающий не только сами растения, но и подготовку/работы) в принципе не может служить проверкой густоты посадки — реальный ограничитель только норма 623-ПП.

## Фаза 3 — Проверка на реальных данных

- [x] **Прогон Service A на одном реальном объекте из «Пилотный проект 20 улиц»** (2026-09-17, `tests/test_service_a_regression.py`) — после DWG→DXF конвертации; сверка отступов АВТОМАТИЗИРОВАНА (не вручную) через 6.1 (`offset_compliance.py`) внутри самого прогона.
- [ ] Прогон на всех 20 объектах, сравнение с «Проектное решение» каждого объекта как ориентиром (это не формальный «эталонный выход» в смысле ТЗ, а фактически выполненный когда-то проект — использовать для сверки правдоподобия, а не как точный golden-output).
- [x] **Проверка открытия итогового DXF в реальном CAD-приложении** (2026-09-24) — результат на реальном объекте («Песчаный переулок») открыт в ZWCAD Viewer (Windows, AutoCAD-совместимый, установлен через winget), без ошибок, схема отображена корректно — подтверждено пользователем визуально. Именно на MosTex.OS/Linux (nanoCAD) — по-прежнему не проверено, нет доступа к реальной машине (см. `docs/LIMITATIONS.md`).
- [x] **Целевой тест на «галлюцинацию»** (2026-09-24, `tests/test_hallucination_guard.py`) — в отличие от юнит-тестов отдельных проверок ОТК, гоняет ПОЛНЫЙ `run_service_a()` на синтетическом DXF с заведомым противоречием (граница участка декларирует 4 м², но реальный водопровод на том же чертеже физически тянется на ~4 км²) — подтверждено: пайплайн не падает, агент ОТК 6.1a честно возвращает `implausible` (не молчаливое доверие декларированной границе), вердикт реально доходит до JSON-отчёта (`otk_boundary_plausibility.verdict`), а не теряется по пути.
- [x] **Повторяемость на реальном объекте, не только на синтетике** (2026-09-17, `tests/test_service_a_regression.py`) — реальный объект («Песчаный переулок») прогнан 5 раз, DXF/отчёт идентичны (за вычетом узкого поимённого списка волатильных ezdxf-метаданных момента сохранения — `$TDCREATE/$TDUPDATE/$VERSIONGUID`+служебная запись версии, см. `docs/SESSION_LOG.md` 2026-09-17). Реальный Ingest (файловая система, DWG-конвертация, парсинг DXF) — не только синтетика.
- [x] **Кросс-платформенная повторяемость (Windows dev vs Linux) — пройдена**: WSL Ubuntu 26.04, те же зафиксированные версии зависимостей, тот же GEOS 3.13.1, 13/13 тестов зелёные, вывод `tests/cross_platform_dump.py` побайтово идентичен на Windows и Linux (`diff --strip-trailing-cr`). См. `docs/ARCHITECTURE.md` §3.1.
- [x] **Внутри Docker-образа проекта** (2026-09-18) — `docker compose run --rm app python -m pytest tests/ -q`: 107 passed, 4 skipped (реальные объекты/ODA-конвертер честно недоступны в образе). Не заменяет WSL-сверку выше (та проверяла именно побайтовую идентичность вывода `cross_platform_dump.py`, эта — что весь набор тестов вообще проходит в целевой среде развёртывания) — обе актуальны, оставлено обе строки. **Осталось**: повторить `cross_platform_dump.py`-подход после каждого содержательного изменения Constraint Engine, не только один раз.
- [x] **Тест на повторяемость Placement Generator** (2026-09-24, `tests/test_placement_generator_reproducibility.py`) — закрывает пробел, явно оставленный `tests/test_reproducibility.py` («Placement Generator ещё не написан, тестировать отдельно, когда появится») и остававшийся непокрытым уже долгое время после того, как генератор был написан. Тот же метод (10+ независимых прогонов, полное структурное равенство `PlacementResult`), но для ОБОИХ алгоритмов генерации кандидатов — сплошная сетка (дерево, `_grid_candidates`) и кластеризация группами (кустарник, `_cluster_candidates`) — оба детерминированы.
- [ ] Подготовить к демо: **не кэш финального ответа**, а скрипт, который реально прогоняет пайплайн на тестовом объекте с нуля вживую перед жюри за разумное время — см. `docs/ARCHITECTURE.md` §3.1a (кэш ≠ алгоритмическая детерминированность).

## Фаза 4 — Документация и сдача (обязательные по ТЗ пункты)

- [x] **Схема процесса** (2026-09-23) — `docs/PROCESS_FLOWCHART.md`: flowchart (Mermaid, допустимая по ТЗ альтернатива BPMN) от входного DWG/DXF до выходного DXF, отражает реально реализованный путь (Service A), с развилками (формат входа, режим автогенерация/проверка человека, замечания ОТК).
- [x] **Документ «Назначение и границы решения»** (2026-09-23) — `docs/SOLUTION_SCOPE.md`: что в MVP, что оставлено на развитие, целевая среда, ссылки на детальные документы.
- [x] **Документ используемых методов/библиотек со ссылками на источники** (2026-09-23) — `docs/METHODS_AND_LIBRARIES.md`: обоснование подхода (правила, не ML), таблица библиотек с версиями и ссылками на PyPI, системные/внешние зависимости (GEOS, ODA File Converter), реестр нормативных источников.
- [x] **Пошаговая инструкция сборки/запуска/проверки под MosTex.OS** (2026-09-23) — `docs/MOSTEX_OS_DEPLOYMENT.md`: пошаговые Docker-команды от сборки образа до получения `result.dxf`/`report.json`, с честной оговоркой — проверено на Ubuntu/WSL и Linux-контейнере, не на самом MosTex.OS (нет доступа к реальной машине). Открытие итогового DXF в реальном CAD-приложении подтверждено (2026-09-24, ZWCAD Viewer на Windows) — именно на MosTex.OS/nanoCAD по-прежнему не проверено.
- [x] **Swagger/OpenAPI** — уже фактически реализовано (FastAPI отдаёт `/docs` из коробки, см. пункт «Веб-UI» выше) — просто не было вычеркнуто.
- [x] **Документ ограничений/допущений/известных ошибок** (2026-09-23) — `docs/LIMITATIONS.md`: сведены в один документ ограничения Constraint Engine, датасета, нереализованные по ТЗ пункты, осознанные инженерные допущения — без переписывания истории расследования (та осталась в `docs/OPEN_QUESTIONS.md`).
- [ ] Презентация (10–20 слайдов, pptx/pdf) — контент подготовлен (`docs/presentation_brief.html`), сам файл слайдов не собран.
- [x] **Финальная проверка документации: нигде не упоминаются внешние личные проекты пользователя** (2026-09-23) — проверено `grep` по всему проекту. **Найдено и исправлено реальное нарушение**: `docs/OPEN_QUESTIONS.md` (запись про геопривязку «Олимпийской деревни») напрямую называла репозиторий другого личного проекта пользователя (`realty_roi_predictor`) — заменено на нейтральную формулировку без идентифицирующего имени. Повторный `grep` — 0 совпадений.

## Фаза 5 (после ядра) — Продуктовая надстройка

- [ ] Plant Catalog Agent (расширенный каталог с прогнозом роста корней).
- [ ] History/Provenance Agent (история посадок, версии прогонов, diff между прогонами).
- [ ] Uploaded-Plan Overlay Agent (загрузка плана + планируемых посадок → карта вариантов размещения).
- [ ] Visualization Agent (бонус — фотореалистичные рендеры после DXF).
